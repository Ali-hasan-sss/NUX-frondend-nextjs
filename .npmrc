# تقليل التعليق على الشبكة وتسريع التثبيت
fetch-timeout=120000
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# استخدام الذاكرة المؤقتة عند توفر الحزم
prefer-offline=true

# إذا استمر التوقف: جرّب إلغاء التعليق على السطر التالي
# legacy-peer-deps=true
